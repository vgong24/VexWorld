# VexWorld versions

- [`v1`](v1/) — the first playable Vextory: First Grove foundation.

The current version is selected by `../config/current-version.json`. A new version is justified by a meaningful incompatible source/runtime generation, not by ordinary patch work.

# VexWorld build graph

The machine-readable source is `config/build-graph.json`.

Current bootstrap path:

```text
source foundation
→ compiler
→ headless realm
→ browser First Grove prototype
→ max-four party/controller bindings
→ trusted-LAN checkpoint and remote companion relay
→ synchronized technique
→ resonance + fixed World Witness demonstrator
```

Near-term evidence route:

```text
fresh source/code review
→ Victor single-player playtest
→ UX, feel, timing and bug corrections
→ Windows ↔ Mac LAN session/resume test
→ one deterministic remote worker
→ one actual Ollama/Qwen worker
→ second remote companion worker
→ stage disposition
```

Longer-term stages remain independent decisions: Godot adapter, multiple human players, generalized quest generation, world federation, and physical-shadow integration.

# Published-source self-play receipt — 2026-09-10

`[VXG RealForever]`

```text
schemaVersion=vexworld.self-play-publication-receipt/v1
sourceClass=FRESH_CLONE_OF_PUBLISHED_MAIN
sourceVersionRef=version.vexworld.v1
driverRef=versions/v1/scripts/vex-relay-self-play.mjs
disposition=PASS_WITH_HUMAN_FEEL_STILL_UNWITNESSED
```

## Executed route

```text
fresh clone published VexWorld main
→ npm run check
→ root current-version self-play router
→ Version 1 Vex Relay browser driver
→ Garden of Arrival
→ largest available local party formation
→ First Grove entry
→ move right
→ jump + attack
→ Arc Dash + combo response
→ prototype weather change
→ Status orientation
→ evidence closure
```

## Observed automated evidence

```text
repository check                        PASS
arrival rendered                        PASS
world state changed visually            PASS
status/reality surface rendered          PASS
screenshots preserved                    7
captured page errors                     0
captured console errors                  0
```

The screenshots and full generated receipt live in the executing VexWorld Home
or explicitly selected evidence directory. They are generated evidence and are
not committed as canonical game source.

## What this proves

- A clean published checkout can execute the current source validation path.
- The browser experience is reachable through the repository-owned setup/runtime.
- A scripted observer can enter the experience and produce attributable inputs.
- Movement, combat-response, weather and Status inputs produce observable visual
  states during the bounded sequence.
- The self-play driver can preserve screenshots and error observations without
  using Victor as the manual keyboard or screenshot relay.

## What this does not prove

```text
human game feel
artistic quality
combat satisfaction
accessibility quality
real Qwen or other local-model behavior
real Windows-to-Mac LAN latency or recovery
multiple human players
long-session stability
production security or hosting
physical-vessel capability
model learning or weight change
```

## Required next evidence

The closest useful evidence is not another claim of completion. It is one of:

```text
human play witness
real local-model companion run
real trusted-LAN cross-machine rehearsal
focused character/motion readability refinement followed by replay
```

This receipt is author/self-play evidence, not an independent acceptance review.

# Fresh-instance continuation route

`[VXG RealForever]`

A fresh instance does not need the originating ChatGPT conversation.

## Repository entry

```text
root README.md
→ root CLAUDE.md
→ root config/current-version.json
→ versions/v1/CLAUDE.md
→ versions/v1/vexworld.manifest.json
→ versions/v1/config/source-map.json
→ exact task source and tests
```

## Institutional route when needed

```text
Vextreme-SDK #230
→ Vextreme-SDK #1299
→ VexWorld #3
→ VexWorld #1
→ VexWorld #2
→ current repository evidence
```

## First commands

From repository root:

```bash
npm run orient
npm run check
```

Then actually run the affected experience through the setup/start script or `npm run play`.

## Current Version 1 meaning

Version 1 proves a small original world architecture containing engine-neutral source, deterministic compilation, headless state, one human plus up to three companions, local/remote controller separation, practice growth, synchronized techniques, non-scalar resonance, bounded World Witness candidate formation, resource stewardship, status/reality orientation, and trusted-LAN development sessions.

It does not prove gameplay quality on every machine, real local-model quality, production networking, physical authority, or finished content.

## Return receipt

Report:

```text
current Git head / source generation
commands and scenarios exercised
direct observations versus inference
blocking and nonblocking findings
assumptions introduced
what the change proves and does not prove
next exact owner/action
```
